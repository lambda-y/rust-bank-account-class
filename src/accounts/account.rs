pub struct Account {
    pub(crate) name: String,
    pub(crate) balance: f64,
    pub(crate) interest_rate: f64
}

// pub trait Account {
//     type Name;
//     type Balance;
//     type InterestRate;
// }